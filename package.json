{
  "name": "cognitiva-docs",
  "private": true,
  "description": "Markdown to navigable docs toolkit",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "serve": "python3 -m http.server 3099"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
